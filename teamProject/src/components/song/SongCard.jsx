import { Link } from 'react-router-dom'
import { Music2, Star } from 'lucide-react'
import { Card } from '../ui/Card'
export function SongCard({ song }) { const stat = song.song_rating_stats?.[0] || {}; return <Link to={`/songs/${song.id}`} className="block h-full"><Card className="group h-full overflow-hidden transition hover:-translate-y-0.5 hover:shadow-md"><div className="aspect-square bg-gradient-to-br from-red-100 via-amber-100 to-orange-200">{song.album_image_url ? <img src={song.album_image_url} alt={`${song.title} 앨범 표지`} className="size-full object-cover" loading="lazy"/> : <div className="grid size-full place-items-center text-red-700"><Music2 size={44}/></div>}</div><div className="p-4"><p className="text-xs font-bold text-red-700">{song.genres?.name || '장르 미정'}</p><h3 className="mt-1 line-clamp-1 font-bold group-hover:text-red-700">{song.title}</h3><p className="mt-1 line-clamp-1 text-sm text-slate-600">{song.artists?.name || '아티스트 미정'}</p><p className="mt-3 flex items-center gap-1 text-sm font-semibold"><Star size={16} className="fill-amber-400 text-amber-500"/>{Number(stat.avg_rating || 0).toFixed(1)} <span className="font-normal text-slate-500">리뷰 {stat.review_count || 0}</span></p></div></Card></Link> }
