import { useState } from 'react'
import { Button } from '../ui/Button'
import { Input } from '../ui/Input'
import { Textarea } from '../ui/Textarea'
import { RatingStars } from './RatingStars'
export function ReviewForm({ initial = {}, onSubmit, submitting }) { const [rating, setRating] = useState(Number(initial.rating) || 5); const [title, setTitle] = useState(initial.title || ''); const [content, setContent] = useState(initial.content || ''); return <form className="space-y-5" onSubmit={(event) => { event.preventDefault(); onSubmit({ rating, title, content }) }}><div><p className="mb-2 text-sm font-semibold">별점 <span className="text-red-700">*</span></p><RatingStars value={rating} onChange={setRating}/><p className="mt-1 text-xs text-slate-500">별의 왼쪽 절반을 누르면 0.5점 단위로 선택할 수 있어요.</p></div><label className="block text-sm font-semibold">제목 <span className="text-red-700">*</span><Input required value={title} onChange={(event) => setTitle(event.target.value)} className="mt-2" placeholder="리뷰 제목을 입력하세요"/></label><label className="block text-sm font-semibold">감상 <span className="text-red-700">*</span><Textarea required value={content} onChange={(event) => setContent(event.target.value)} className="mt-2" placeholder="음악을 듣고 느낀 점을 자유롭게 적어주세요."/></label><Button type="submit" disabled={submitting} className="w-full">{submitting ? '저장 중...' : '리뷰 저장하기'}</Button></form> }
