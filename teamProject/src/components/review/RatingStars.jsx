import { Star } from 'lucide-react'
export function RatingStars({ value, onChange, label = '별점' }) { return <div aria-label={`${label} ${value}점`} className="flex items-center gap-1">{[1, 2, 3, 4, 5].map((star) => <span key={star} className="relative inline-flex"><button type="button" disabled={!onChange} onClick={() => onChange(star - 0.5)} aria-label={`${star - 0.5}점 선택`} className="absolute inset-y-0 left-0 z-10 w-1/2 disabled:cursor-default"/><button type="button" disabled={!onChange} onClick={() => onChange(star)} aria-label={`${star}점 선택`} className="relative z-0 disabled:cursor-default"><Star size={25} className={star <= value ? 'fill-amber-400 text-amber-500' : star - 0.5 === value ? 'fill-amber-200 text-amber-500' : 'text-stone-300'} /></button></span>)}<span className="ml-2 text-sm font-bold text-slate-700">{Number(value).toFixed(1)}</span></div> }
